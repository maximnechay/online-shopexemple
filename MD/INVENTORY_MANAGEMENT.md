# Система управления складом (Inventory Management)

## Обзор

Склад управляется в зависимости от способа оплаты:

### 🔄 Жизненный цикл заказа

```
Создание заказа → Оплата → Уменьшение склада → Обработка → Доставка
      ↓              ↓              ↓
   pending      paid/completed   processing
```

## ⚡ Ключевые принципы

1. **Склад НЕ уменьшается при добавлении в корзину**
2. **Склад НЕ уменьшается при создании заказа**
3. **Склад уменьшается ТОЛЬКО когда:**
   - Платёж подтверждён платёжной системой (Stripe/PayPal)
   - Администратор вручную подтверждает cash заказ

## 📦 Способы оплаты

### 1. Оплата картой (Stripe)

```
1. Пользователь оформляет заказ → статус: pending
2. Stripe webhook получает checkout.session.completed
3. Система проверяет payment_id (идемпотентность)
4. Уменьшается склад атомарно
5. Статус меняется: pending → paid → processing
```

**Файлы:**
- `/api/checkout` - создание Stripe сессии
- `/api/webhooks/stripe` - обработка успешной оплаты

### 2. PayPal

```
1. Пользователь оформляет заказ → статус: pending
2. PayPal webhook получает CHECKOUT.ORDER.APPROVED
3. Система проверяет payment_id
4. Уменьшается склад
5. Статус меняется: pending → paid → processing
```

**Файлы:**
- `/api/paypal/create-order` - создание PayPal заказа
- `/api/webhooks/paypal` - обработка успешной оплаты

### 3. Наличные (Cash)

```
1. Пользователь оформляет заказ → статус: pending
   - Склад НЕ уменьшается
2. Администратор получает уведомление
3. Администратор подтверждает получение оплаты
4. Уменьшается склад
5. Статус меняется: pending → paid → processing
```

**Файлы:**
- `/api/orders` - создание заказа
- `/api/admin/orders/[orderId]/confirm-payment` - подтверждение оплаты админом

## 🛡️ Защита от проблем

### Идемпотентность

Каждый заказ имеет уникальный `payment_id`:
- Stripe: session_id
- PayPal: order_id
- Cash: CASH-{timestamp}-{random}

Это предотвращает:
- Двойное списание товара
- Обработку дубликатов webhook'ов
- Гонки при одновременных запросах

### Атомарность

Функция `decreaseStock()`:
1. Проверяет наличие всех товаров
2. Если хотя бы одного не хватает - откатывается полностью
3. Использует optimistic locking (проверка stock перед обновлением)
4. Логирует каждое изменение в `stock_logs`

### Проверки на уровне БД

```sql
-- Склад не может быть отрицательным
CHECK (stock_quantity >= 0)

-- Количество в заказе должно быть > 0
CHECK (quantity > 0)

-- payment_id уникален
UNIQUE (payment_id)
```

## 📊 Аудит и логирование

Все изменения склада записываются в таблицу `stock_logs`:

```sql
CREATE TABLE stock_logs (
    id UUID PRIMARY KEY,
    product_id UUID,
    order_id UUID,
    event_type TEXT, -- purchase, refund, manual_adjust, cancelled
    quantity_change INTEGER, -- отрицательное для покупок, положительное для возвратов
    stock_before INTEGER,
    stock_after INTEGER,
    payment_id TEXT,
    notes TEXT,
    created_at TIMESTAMPTZ
);
```

### Типы событий:

- `purchase` - покупка (уменьшение склада)
- `refund` - возврат (увеличение склада)
- `manual_adjust` - ручная корректировка админом
- `cancelled` - отмена заказа до оплаты (склад не трогался)

## 🔧 API Endpoints

### Для пользователей:

```typescript
POST /api/orders
// Создаёт заказ, проверяет наличие, НЕ уменьшает склад

POST /api/checkout
// Stripe checkout session

POST /api/paypal/create-order
// PayPal order creation
```

### Для админов:

```typescript
POST /api/admin/orders/[orderId]/confirm-payment
// Подтверждает cash платёж и уменьшает склад

PATCH /api/admin/orders/[orderId]
// Обновляет статус заказа (не трогает склад)
```

### Webhooks:

```typescript
POST /api/webhooks/stripe
// Обрабатывает Stripe события (payment success, refund)

POST /api/webhooks/paypal
// Обрабатывает PayPal события
```

## 💡 Использование в коде

### Проверка наличия

```typescript
import { checkAvailability } from '@/lib/inventory/stock-manager';

const availability = await checkAvailability([
  { productId: 'uuid', quantity: 2 },
  { productId: 'uuid2', quantity: 1 }
]);

if (!availability.available) {
  console.log('Недостаточно товара:', availability.unavailableItems);
}
```

### Уменьшение склада

```typescript
import { decreaseStock } from '@/lib/inventory/stock-manager';

const result = await decreaseStock(
  [{ productId: 'uuid', quantity: 2, notes: 'Order #123' }],
  orderId,
  paymentId
);

if (!result.success) {
  console.error('Ошибка:', result.error);
}
```

### Возврат на склад (refund)

```typescript
import { increaseStock } from '@/lib/inventory/stock-manager';

const result = await increaseStock(
  [{ productId: 'uuid', quantity: 2, notes: 'Refund for order #123' }],
  orderId,
  paymentId
);
```

## 🚨 Обработка ошибок

### Недостаточно товара

При создании заказа:
```json
{
  "error": "Nicht genügend Lagerbestand",
  "unavailableItems": [
    {
      "productId": "uuid",
      "productName": "Product Name",
      "requested": 5,
      "inStock": 2
    }
  ]
}
```

### При оплате

Если между созданием заказа и оплатой кто-то купил товар:
1. Webhook попытается уменьшить склад
2. Получит ошибку "Insufficient stock"
3. Заказ остаётся в статусе pending
4. Администратор видит проблему в админ-панели
5. Нужно связаться с клиентом

## 🎯 Админ-панель

### Действия администратора:

1. **Просмотр заказа** → `/admin/orders/[orderId]`

2. **Для cash заказов:**
   - Видна зелёная кнопка "Zahlung bestätigen"
   - При нажатии уменьшается склад
   - Статус меняется на paid

3. **Просмотр истории склада:**
   - Можно добавить страницу `/admin/stock-logs`
   - Показывает все изменения с фильтрами

4. **Ручная корректировка:**
   ```typescript
   import { adjustStock } from '@/lib/inventory/stock-manager';
   
   await adjustStock(
     productId,
     newStockValue,
     'Инвентаризация: обнаружен брак'
   );
   ```

## 📝 Миграция БД

Файл: `supabase/migrations/add_inventory_management.sql`

Включает:
- ✅ Добавление `payment_id UNIQUE` в orders
- ✅ Создание таблицы `stock_logs`
- ✅ Constraints для stock_quantity >= 0
- ✅ Индексы для производительности
- ✅ RLS политики

## 🧪 Тестирование

### Сценарий 1: Cash заказ

```bash
# 1. Создать заказ
curl -X POST /api/orders -d '{...}' 
# Проверить: stock_quantity НЕ изменился

# 2. Админ подтверждает
curl -X POST /api/admin/orders/[id]/confirm-payment
# Проверить: stock_quantity уменьшился, создан stock_log
```

### Сценарий 2: Stripe

```bash
# 1. Создать заказ с Stripe
# 2. Webhook придёт автоматически
# 3. Проверить stock_quantity и stock_logs
```

### Сценарий 3: Недостаток товара

```bash
# 1. Товара 1 штука
# 2. Два человека заказывают одновременно по 1 штуке
# Результат: Один получит заказ, второй - ошибку
```

## 🔄 Возвраты (Refunds)

TODO: Добавить обработку в webhooks:

```typescript
// В /api/webhooks/stripe
if (event.type === 'charge.refunded') {
  // Найти заказ по payment_id
  // Проверить статус (должен быть paid)
  // Увеличить склад через increaseStock()
  // Сменить статус на refunded
}
```

## 📖 Дополнительно

- [DATABASE.md](./DATABASE.md) - структура БД
- [API.md](./API.md) - документация API
- [STRIPE_WEBHOOK_SETUP.md](./STRIPE_WEBHOOK_SETUP.md) - настройка Stripe
