# Abandoned Cart Recovery - Полное руководство 🛒💰

## ✅ Что реализовано

### 1. База данных
- ✅ Таблица `abandoned_carts` с полями для корзин, токенов, email статусов
- ✅ Таблица `abandoned_cart_stats` для ежедневной статистики
- ✅ SQL функции для работы с корзинами
- ✅ RLS политики безопасности
- ✅ Индексы для производительности

### 2. Backend API
- ✅ `POST /api/abandoned-cart/save` - сохранение брошенной корзины
- ✅ `GET /api/abandoned-cart/recover?token=xxx` - получение корзины по токену
- ✅ `POST /api/abandoned-cart/recover` - пометка корзины как восстановленной
- ✅ `POST /api/abandoned-cart/send-emails` - cron endpoint для отправки emails

### 3. Email система
- ✅ 3 красивых HTML шаблона (1ч, 24ч, 3дня)
- ✅ Персонализация: имя, фото товаров, цены
- ✅ Генерация персональных купонов 10%
- ✅ Интеграция с Resend API

### 4. Frontend
- ✅ React хук `useAbandonedCartTracking` для автоматического сохранения
- ✅ Страница восстановления `/cart/recover`
- ✅ Отслеживание неактивности (5 минут)
- ✅ Сохранение при закрытии страницы

### 5. Автоматизация
- ✅ Vercel Cron Jobs конфигурация
- ✅ Автоматическая отправка emails каждый час

---

## 🚀 Установка и настройка

### Шаг 1: Настройка базы данных

1. **Запустите SQL миграцию** в Supabase:
   ```sql
   -- Файл: supabase/migrations/20241204_abandoned_carts.sql
   ```

2. **Проверьте создание таблиц**:
   ```sql
   SELECT * FROM abandoned_carts LIMIT 1;
   SELECT * FROM abandoned_cart_stats LIMIT 1;
   ```

### Шаг 2: Настройка переменных окружения

Добавьте в `.env.local`:

```env
# Resend API для отправки emails
RESEND_API_KEY=re_your_api_key_here

# Секретный ключ для защиты cron endpoint
CRON_SECRET=your_random_secret_key_here

# URL сайта (для ссылок в emails)
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Email отправителя
EMAIL_FROM=noreply@your-domain.com

# Supabase (если еще не настроено)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

### Шаг 3: Получить Resend API ключ

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Подтвердите домен или используйте их тестовый домен
3. Создайте API ключ в Settings → API Keys
4. Скопируйте ключ в `RESEND_API_KEY`

### Шаг 4: Интеграция хука отслеживания

Добавьте хук в главный layout или компонент корзины:

```tsx
// app/layout.tsx
import { useAbandonedCartTracking } from '@/lib/hooks/useAbandonedCartTracking';

function CartTracker() {
    useAbandonedCartTracking();
    return null;
}

export default function RootLayout({ children }) {
    return (
        <html>
            <body>
                <CartTracker />
                {children}
            </body>
        </html>
    );
}
```

### Шаг 5: Настройка Vercel Cron Jobs

1. **Деплой на Vercel** с `vercel.json`
2. **Проверьте Cron Jobs** в Vercel Dashboard → Settings → Cron Jobs
3. **Добавьте переменную окружения** `CRON_SECRET` в Vercel

### Шаг 6: Тестирование системы

#### Тест 1: Сохранение брошенной корзины

```bash
curl -X POST http://localhost:3000/api/abandoned-cart/save \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "cartItems": [
      {
        "product_id": "123",
        "name": "Test Product",
        "slug": "test-product",
        "price": 29.99,
        "quantity": 1,
        "image": "https://example.com/image.jpg"
      }
    ],
    "cartTotal": 29.99
  }'
```

#### Тест 2: Восстановление корзины

```bash
# Получить токен из БД
SELECT recovery_token FROM abandoned_carts LIMIT 1;

# Протестировать recovery
curl http://localhost:3000/api/abandoned-cart/recover?token=YOUR_TOKEN
```

#### Тест 3: Отправка emails (вручную)

```bash
curl -X POST http://localhost:3000/api/abandoned-cart/send-emails \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_CRON_SECRET" \
  -d '{"emailType": "1h"}'
```

---

## 📧 Email триггеры

### Email #1: Через 1 час
- **Тема**: "Sie haben etwas in Ihrem Warenkorb vergessen! 🛍️"
- **Содержание**: Напоминание о товарах в корзине
- **CTA**: Кнопка "Zurück zum Warenkorb"

### Email #2: Через 24 часа
- **Тема**: "🎁 Exklusiv für Sie: 10% Rabatt auf Ihren Warenkorb!"
- **Содержание**: Персональный купон на 10%
- **Особенности**: 
  - Генерация уникального купона
  - Показ новой цены со скидкой
  - Срок действия 48 часов

### Email #3: Через 3 дня
- **Тема**: "⏰ Letzte Chance! Ihre Produkte warten noch auf Sie"
- **Содержание**: Последнее напоминание
- **Особенности**: 
  - Напоминание о купоне (если был)
  - Social proof
  - Срочность

---

## 🔧 Настройка Cron Jobs

### Vercel Cron (Рекомендуется)

Уже настроено в `vercel.json`:
```json
{
  "crons": [
    {
      "path": "/api/abandoned-cart/send-emails",
      "schedule": "0 * * * *"
    }
  ]
}
```

Cron запускается **каждый час** и отправляет все типы emails:
- 1h emails для корзин старше 1 часа
- 24h emails для корзин старше 24 часов
- 3d emails для корзин старше 3 дней

### Альтернатива: Внешний Cron

Если Vercel Cron недоступен, используйте:

**1. Cron-job.org**
- URL: `https://your-domain.com/api/abandoned-cart/send-emails`
- Method: POST
- Headers: `Authorization: Bearer YOUR_CRON_SECRET`
- Body: `{"emailType": "1h"}` (создайте 3 задачи)
- Schedule: Каждый час

**2. GitHub Actions**
```yaml
# .github/workflows/abandoned-cart.yml
name: Send Abandoned Cart Emails
on:
  schedule:
    - cron: '0 * * * *'
jobs:
  send-emails:
    runs-on: ubuntu-latest
    steps:
      - name: Send 1h emails
        run: |
          curl -X POST ${{ secrets.SITE_URL }}/api/abandoned-cart/send-emails \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            -d '{"emailType": "1h"}'
```

---

## 📊 Мониторинг и аналитика

### Просмотр брошенных корзин

```sql
-- Все активные брошенные корзины
SELECT 
    id, email, cart_total, created_at,
    email_1h_sent, email_24h_sent, email_3d_sent
FROM abandoned_carts
WHERE recovered_at IS NULL
ORDER BY created_at DESC;

-- Статистика за сегодня
SELECT * FROM abandoned_cart_stats
WHERE date = CURRENT_DATE;
```

### Метрики для отслеживания

1. **Conversion Rate**: Процент восстановленных корзин
2. **Email Effectiveness**: Какой email приносит больше конверсий
3. **Average Cart Value**: Средняя стоимость брошенной корзины
4. **Recovery Time**: Как быстро восстанавливают корзины

### Обновление статистики

Запускайте ежедневно:
```sql
SELECT update_daily_cart_stats();
```

---

## 🎯 Интеграция с купонной системой

### Вариант 1: Использовать существующую систему купонов

Если у вас есть таблица `coupons`, добавьте купоны автоматически:

```typescript
// В функции generateCouponCode()
const couponCode = `RECOVER${cartId.substring(0, 4)}${randomPart}`;

// Сохранить в таблицу coupons
await supabase.from('coupons').insert({
    code: couponCode,
    discount_type: 'percentage',
    discount_value: 10,
    valid_until: new Date(Date.now() + 48 * 60 * 60 * 1000),
    usage_limit: 1,
    created_for_cart: cartId,
});
```

### Вариант 2: Простая проверка купона

В API checkout проверяйте купон из `abandoned_carts`:

```typescript
// app/api/checkout/route.ts
const { data: cart } = await supabase
    .from('abandoned_carts')
    .select('coupon_code')
    .eq('coupon_code', couponInput)
    .single();

if (cart) {
    // Применить скидку 10%
    discount = subtotal * 0.1;
}
```

---

## 🐛 Troubleshooting

### Проблема: Emails не отправляются

**Решение:**
1. Проверьте `RESEND_API_KEY` в переменных окружения
2. Проверьте логи Vercel: `vercel logs`
3. Проверьте лимиты Resend аккаунта
4. Убедитесь что домен подтвержден в Resend

### Проблема: Cron не запускается

**Решение:**
1. Проверьте Vercel Dashboard → Cron Jobs
2. Убедитесь что `vercel.json` закоммичен
3. Проверьте логи: Vercel Dashboard → Deployments → Functions
4. Тестируйте вручную: `curl -X POST ...`

### Проблема: Корзины не сохраняются

**Решение:**
1. Проверьте что хук `useAbandonedCartTracking` интегрирован
2. Откройте консоль браузера - должны быть логи
3. Проверьте что пользователь авторизован или есть `guest_email`
4. Проверьте RLS политики в Supabase

### Проблема: Токен восстановления невалиден

**Решение:**
1. Токены уникальны и действительны вечно (пока не использованы)
2. Проверьте что функция `generate_recovery_token()` создана
3. Проверьте что в таблице есть запись с этим токеном

---

## 📈 Оптимизация и улучшения

### Повышение конверсии

1. **A/B тестирование email шаблонов**
   - Тестируйте разные темы писем
   - Пробуйте разные размеры скидки (5%, 10%, 15%)

2. **Персонализация**
   - Используйте имя клиента в письмах
   - Показывайте похожие товары

3. **Urgency (срочность)**
   - Добавьте таймер "Осталось 24 часа"
   - Показывайте ограниченное количество товара

### Расширение функционала

1. **SMS уведомления**
   - Интеграция с Twilio
   - Отправка SMS через 12 часов

2. **Push уведомления**
   - Web Push API
   - Напоминание через браузер

3. **Ремаркетинг**
   - Facebook/Instagram Pixel
   - Google Ads remarketing

4. **Сегментация**
   - VIP клиенты - больше скидка
   - Новые клиенты - бесплатная доставка

---

## 💰 ROI и метрики

### Ожидаемые результаты

- **Recovery Rate**: 20-30% брошенных корзин
- **Email Open Rate**: 40-50%
- **Click Rate**: 15-20%
- **Окупаемость**: 1-2 месяца

### Расчет ROI

```
Пример:
- Брошенных корзин в месяц: 500
- Средний чек: €50
- Recovery rate: 25% (125 заказов)
- Дополнительный доход: 125 × €50 = €6,250/месяц
- Затраты на Resend: ~€20/месяц
- ROI: 312x (31,200%)
```

---

## 📝 Checklist запуска

- [ ] SQL миграция выполнена
- [ ] Таблицы созданы в Supabase
- [ ] RESEND_API_KEY настроен
- [ ] CRON_SECRET сгенерирован и добавлен
- [ ] Email шаблоны работают (тест)
- [ ] Хук `useAbandonedCartTracking` интегрирован
- [ ] Страница `/cart/recover` доступна
- [ ] Vercel Cron Jobs активен
- [ ] Тестовая корзина сохранена
- [ ] Тестовый email отправлен
- [ ] Восстановление корзины работает
- [ ] Купоны применяются в checkout
- [ ] Мониторинг настроен
- [ ] Документация прочитана командой

---

## 🎓 Дополнительные ресурсы

### Документация
- [Resend API](https://resend.com/docs)
- [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs)
- [Supabase RLS](https://supabase.com/docs/guides/auth/row-level-security)

### Инструменты
- [Email тестирование](https://www.emailonacid.com/)
- [Cron expression generator](https://crontab.guru/)
- [HTML email templates](https://github.com/leemunroe/responsive-html-email-template)

---

## 🚀 Следующие шаги

1. **Запустите SQL миграцию** → Создайте таблицы
2. **Настройте Resend** → Получите API ключ
3. **Интегрируйте хук** → Добавьте отслеживание
4. **Деплой на Vercel** → Активируйте Cron
5. **Тестируйте** → Создайте тестовую корзину
6. **Мониторьте** → Отслеживайте метрики
7. **Оптимизируйте** → Улучшайте конверсию

---

**Система готова к запуску!** 🎉

При возникновении вопросов - проверьте логи, документацию или обратитесь к этому руководству.

**Удачи с восстановлением брошенных корзин!** 💰📈
