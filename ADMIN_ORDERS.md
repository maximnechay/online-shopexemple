# 📦 Админка для управления заказами

## Создано

### Страницы

1. **`/admin/orders`** - Список всех заказов
   - Фильтрация по статусу
   - Поиск по номеру заказа, имени или email
   - Статистика заказов
   - Обновление в реальном времени

2. **`/admin/orders/[orderId]`** - Детальный просмотр заказа
   - Просмотр информации о клиенте
   - Список заказанных товаров
   - Адрес доставки
   - Информация об оплате
   - Редактирование статусов
   - Добавление заметок
   - Кнопка отправки email вручную

### API Endpoints

1. **`GET /api/admin/orders`**
   - Получить все заказы
   - Сортировка по дате создания (новые первыми)

2. **`GET /api/admin/orders/[orderId]`**
   - Получить детальную информацию о заказе
   - Включает позиции заказа

3. **`PATCH /api/admin/orders/[orderId]`**
   - Обновить статус заказа
   - Обновить статус оплаты
   - Добавить/изменить заметки
   - **Автоматически отправляет email** если статус = `processing` и оплата = `completed`

4. **`DELETE /api/admin/orders/[orderId]`** (опционально)
   - Удалить заказ

## Функционал

### Управление статусами

**Статус заказа:**
- 🟡 `pending` - Ausstehend (Ожидает обработки)
- 🔵 `processing` - In Bearbeitung (В обработке)
- 🟣 `shipped` - Versandt (Отправлен)
- 🟢 `delivered` - Zugestellt (Доставлен)
- 🔴 `cancelled` - Storniert (Отменен)

**Статус оплаты:**
- ⚪ `pending` - Ausstehend (Ожидает оплаты)
- 🟢 `completed` - Bezahlt (Оплачен)
- 🔴 `failed` - Fehlgeschlagen (Ошибка)
- 🟠 `refunded` - Erstattet (Возврат)

### Автоматическая отправка Email

При обновлении заказа админом:

1. Если `status` = `processing` + `payment_status` = `completed`
2. Система автоматически отправит email:
   - Клиенту - подтверждение заказа
   - Админу - уведомление о новом заказе

### Ручная отправка Email

Кнопка **"E-Mail senden"** на странице детального просмотра:
- Отправляет email клиенту и админу
- Работает независимо от статуса
- Полезно если email не отправился автоматически

## Использование

### Обработка заказа с оплатой наличными

1. Откройте `/admin/orders`
2. Найдите заказ со статусом 🟡 `pending`
3. Нажмите "Details"
4. Измените:
   - **Zahlungsstatus** → `Bezahlt` (completed)
   - **Bestellstatus** → `In Bearbeitung` (processing)
5. Нажмите **"Speichern"**
6. ✅ Email автоматически отправятся клиенту!

### Просмотр информации о заказе

На странице детального просмотра видно:
- 👤 Информация о клиенте (имя, email, телефон)
- 📦 Список товаров с ценами
- 📍 Адрес доставки (если доставка)
- 💳 Способ оплаты (Stripe ID или PayPal ID)
- 📝 Внутренние заметки

### Фильтрация и поиск

**Фильтры:**
- Все статусы
- По конкретному статусу

**Поиск:**
- По номеру заказа
- По имени клиента
- По email клиенту

## Доступ

URL: `/admin/orders`

⚠️ **Важно:** Добавьте проверку авторизации для production!

## Интеграция с Email системой

### Когда отправляются email:

| Способ оплаты | Момент отправки |
|---|---|
| PayPal | ✅ Автоматически после оплаты |
| Stripe (production) | ✅ Через webhook после оплаты |
| Stripe (local dev) | ⚠️ Требует Stripe CLI |
| Cash (наличные) | 👤 После подтверждения админом |

### Для Cash заказов:

1. Клиент оформляет заказ с оплатой "наличными"
2. Заказ создается со статусом `pending`
3. **Админ проверяет оплату физически**
4. Админ обновляет статус → `completed` + `processing`
5. ✅ Email отправляются автоматически!

## Статистика

На главной странице `/admin/orders` отображается:
- Общее количество заказов
- Количество ожидающих обработки
- В обработке
- Отправленных
- Доставленных

---

**Готово к использованию!** 🎉

Теперь вы можете управлять всеми заказами из админ-панели.
