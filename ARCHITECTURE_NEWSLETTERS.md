# 📊 Архитектура системы рассылок

## Визуальная схема

```
┌─────────────────────────────────────────────────────────────────┐
│                        ADMIN DASHBOARD                          │
│                         /admin                                  │
└───────────────┬─────────────────────────┬───────────────────────┘
                │                         │
                ▼                         ▼
    ┌───────────────────┐     ┌───────────────────┐
    │  Email-Rассылка   │     │  SMS-Rассылка     │
    │  /admin/newsletter│     │  /admin/sms       │
    └─────────┬─────────┘     └─────────┬─────────┘
              │                          │
              ▼                          ▼
┌─────────────────────────┐  ┌─────────────────────────┐
│  newsletter/page.tsx    │  │  sms/page.tsx           │
│  - Форма темы/текста    │  │  - Форма SMS (160 char) │
│  - HTML-поддержка       │  │  - Счетчик символов     │
│  - Предпросмотр         │  │  - Предпросмотр         │
│  - Статистика           │  │  - Статистика           │
└───────────┬─────────────┘  └───────────┬─────────────┘
            │                            │
            │ POST                       │ POST
            ▼                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                           API ROUTES                            │
├─────────────────────────────┬───────────────────────────────────┤
│  /api/admin/newsletter/     │  /api/admin/sms/                  │
│                             │                                   │
│  ├─ recipients/route.ts     │  ├─ recipients/route.ts          │
│  │  GET: Fetch email list   │  │  GET: Fetch phone list        │
│  │                          │  │                                │
│  └─ send/route.ts           │  └─ send/route.ts                │
│     POST: Send emails       │     POST: Send SMS               │
└───────────┬─────────────────┴────────────┬──────────────────────┘
            │                              │
            │ Query                        │ Query
            ▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SUPABASE DATABASE                          │
├───────────────────────────────┬─────────────────────────────────┤
│  profiles                     │  newsletter_subscribers         │
│  ├─ email                     │  ├─ email                       │
│  ├─ full_name                 │  └─ created_at                  │
│  ├─ newsletter_enabled ✅     │                                 │
│  └─ phone (для SMS)           │                                 │
└───────────────────────────────┴─────────────────────────────────┘
            │                              │
            │ Send via                     │ Send via
            ▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                            │
├─────────────────────────────┬───────────────────────────────────┤
│  📧 RESEND                  │  📱 TWILIO (опционально)          │
│  ├─ Email delivery          │  ├─ SMS delivery                  │
│  ├─ HTML templates          │  ├─ 160 char limit                │
│  ├─ Analytics               │  ├─ Pay per SMS                   │
│  └─ Free: 3k/month          │  └─ ~€0.075/SMS                   │
└─────────────────────────────┴───────────────────────────────────┘
            │                              │
            ▼                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        END USERS                                │
│  👤 Customers receive emails & SMS                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Email-Рассылка

```
1. Admin opens /admin/newsletter
                ↓
2. Page fetches recipients
   GET /api/admin/newsletter/recipients
                ↓
3. Query Supabase:
   - profiles WHERE newsletter_enabled = true
   - newsletter_subscribers (all)
                ↓
4. Deduplicate by email
                ↓
5. Display count in UI (e.g., "152 recipients")
                ↓
6. Admin writes subject & message (HTML)
                ↓
7. Admin clicks "E-Mails senden"
                ↓
8. POST /api/admin/newsletter/send
   {subject, message, recipients[]}
                ↓
9. For each recipient:
   - Generate HTML email from template
   - Send via Resend API
                ↓
10. Resend delivers emails
                ↓
11. Response: {success, stats: {total, successful, failed}}
                ↓
12. UI shows success message
```

---

## Data Flow: SMS-Рассылка

```
1. Admin opens /admin/sms
                ↓
2. Page fetches recipients
   GET /api/admin/sms/recipients
                ↓
3. Query Supabase:
   - profiles WHERE newsletter_enabled = true AND phone IS NOT NULL
                ↓
4. Display count in UI (e.g., "47 recipients")
                ↓
5. Admin writes message (max 160 chars)
                ↓
6. Admin clicks "SMS senden"
                ↓
7. POST /api/admin/sms/send
   {message, recipients[]}
                ↓
8. For each recipient:
   - Send via Twilio API (if configured)
   - OR log in console (demo mode)
                ↓
9. Twilio delivers SMS
                ↓
10. Response: {success, stats: {total, successful, failed}}
                ↓
11. UI shows success message
```

---

## Environment Variables

```
.env.local
├─ Email-система
│  ├─ RESEND_API_KEY=re_xxxxx...          # ✅ Обязательно
│  ├─ EMAIL_FROM=Beauty Salon <...>       # ✅ Обязательно
│  └─ NEXT_PUBLIC_SITE_URL=...            # Для ссылок
│
├─ SMS-система (опционально)
│  ├─ TWILIO_ACCOUNT_SID=ACxxxxx...       # Для SMS
│  ├─ TWILIO_AUTH_TOKEN=...               # Для SMS
│  └─ TWILIO_PHONE_NUMBER=+49...          # Для SMS
│
└─ Supabase (уже настроено)
   ├─ NEXT_PUBLIC_SUPABASE_URL=...
   └─ NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

---

## Database Schema

```sql
-- profiles (существующая таблица)
CREATE TABLE profiles (
    id UUID PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    full_name VARCHAR(255),
    phone VARCHAR(20),                    -- для SMS
    newsletter_enabled BOOLEAN DEFAULT false,  -- флаг подписки
    role VARCHAR(50) DEFAULT 'customer',
    created_at TIMESTAMP DEFAULT NOW()
);

-- newsletter_subscribers (существующая таблица)
CREATE TABLE newsletter_subscribers (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    email VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Опционально: можно добавить phone в newsletter_subscribers
ALTER TABLE newsletter_subscribers 
ADD COLUMN phone VARCHAR(20);
```

---

## File Structure

```
beauty-salon-shop/
├── app/
│   ├── admin/
│   │   ├── page.tsx                     # Dashboard с карточками
│   │   ├── newsletter/
│   │   │   └── page.tsx                 # ✅ Email-рассылка UI
│   │   └── sms/
│   │       └── page.tsx                 # ✅ SMS-рассылка UI
│   └── api/
│       └── admin/
│           ├── newsletter/
│           │   ├── recipients/
│           │   │   └── route.ts         # ✅ GET email list
│           │   └── send/
│           │       └── route.ts         # ✅ POST send emails
│           └── sms/
│               ├── recipients/
│               │   └── route.ts         # ✅ GET phone list
│               └── send/
│                   └── route.ts         # ✅ POST send SMS
├── lib/
│   └── email/
│       ├── send.ts                      # Email functions (для заказов)
│       └── templates/                   # Email templates (для заказов)
├── docs/
│   ├── EMAIL_NEWSLETTER.md              # ✅ Полная документация email
│   ├── QUICK_EMAIL_GUIDE.md             # ✅ Быстрый старт email
│   ├── SMS_SETUP.md                     # ✅ Настройка SMS
│   ├── NEWSLETTER_COMPARISON.md         # ✅ Email vs SMS
│   └── NEWSLETTERS_READY.md             # ✅ Итоговое описание
└── .env.local                           # Environment variables
```

---

## Security & Permissions

```
┌─────────────────────────────────────────────────────────────┐
│  /admin/newsletter & /admin/sms                             │
│  Доступ: ТОЛЬКО администраторы (role = 'admin')            │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  middleware.ts проверяет auth   │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  API routes проверяют role      │
        └─────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │  Supabase RLS защищает данные   │
        └─────────────────────────────────┘
```

---

## Cost Comparison

```
┌────────────────────────────────────────────────────────────────┐
│                      Email (Resend)                            │
├────────────────────────────────────────────────────────────────┤
│  100 recipients × 4 emails/month = 400 emails                  │
│  Cost: €0/month (Free tier)                                    │
│                                                                │
│  1000 recipients × 4 emails/month = 4000 emails                │
│  Cost: ~€20/month (Paid tier)                                  │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                      SMS (Twilio)                              │
├────────────────────────────────────────────────────────────────┤
│  100 recipients × 2 SMS/month = 200 SMS                        │
│  Cost: €15/month (€0.075 per SMS)                              │
│                                                                │
│  1000 recipients × 2 SMS/month = 2000 SMS                      │
│  Cost: €150/month (€0.075 per SMS)                             │
└────────────────────────────────────────────────────────────────┘

RECOMMENDATION:
├─ Use EMAIL for regular marketing (cheap/free)
└─ Use SMS for urgent notifications (expensive but effective)
```

---

## Performance Metrics

```
Email-Рассылка:
├─ Open Rate: 20-30% (industry average)
├─ Click Rate: 2-5%
├─ Delivery Time: Instant - 5 minutes
└─ Cost per email: €0.0007 (paid tier)

SMS-Рассылка:
├─ Open Rate: 98% (!!)
├─ Read Rate: 90%+ within 3 minutes
├─ Delivery Time: Instant
└─ Cost per SMS: €0.075
```

---

## Integration Status

```
✅ READY:
├─ Email frontend UI
├─ Email API endpoints
├─ Email HTML templates
├─ Resend integration
├─ Database queries
├─ Admin dashboard cards
├─ Complete documentation
└─ Error handling

⚙️ PARTIAL (SMS):
├─ SMS frontend UI
├─ SMS API endpoints
├─ Database queries
└─ Demo mode (logging)

❌ REQUIRES SETUP (SMS):
└─ Twilio integration (30 minutes setup)
```

---

## Quick Start Commands

```bash
# 1. Запустить dev server
npm run dev

# 2. Открыть админку
open http://localhost:3000/admin

# 3. Открыть Email-рассылку
open http://localhost:3000/admin/newsletter

# 4. (Опционально) Настроить SMS
npm install twilio
# Then follow SMS_SETUP.md

# 5. Проверить email в Resend
open https://resend.com/emails

# 6. Проверить логи
# F12 → Console в браузере
```

---

## Next Steps

```
IMMEDIATE (Email ready now):
1. Open /admin/newsletter
2. Write your first email
3. Send to subscribers
4. Check Resend dashboard

OPTIONAL (SMS - if needed):
1. Read SMS_SETUP.md
2. Sign up for Twilio
3. Add credentials to .env.local
4. Install twilio package
5. Uncomment code in send/route.ts

FUTURE ENHANCEMENTS:
- [ ] Unsubscribe link in emails
- [ ] Template library
- [ ] Scheduled sends (cron)
- [ ] A/B testing
- [ ] Advanced analytics
- [ ] Personalization (dynamic content)
```

---

**Система готова к использованию! 🚀**

Откройте `/admin/newsletter` и начните отправлять email-рассылки прямо сейчас!
